os:   linux
dist: focal
language: python
python:
    - "3.6"
    - "3.7"
    - "3.8"
    - "3.9"

git:
  submodules: false

install:
  - sudo apt update
  - sudo apt install gdb-multiarch binutils-riscv64-unknown-elf
  - pip install -r requirements.txt

script:
  - pytest
